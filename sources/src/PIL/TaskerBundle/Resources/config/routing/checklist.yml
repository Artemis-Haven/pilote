checklist:
    pattern:  /
    defaults: { _controller: "PILTaskerBundle:CheckList:index" }

checklist_show:
    pattern:  /{id}/show
    defaults: { _controller: "PILTaskerBundle:CheckList:show" }

checklist_new:
    pattern:  /new
    defaults: { _controller: "PILTaskerBundle:CheckList:new" }

checklist_create:
    pattern:  /create
    defaults: { _controller: "PILTaskerBundle:CheckList:create" }
    requirements: { _method: post }

checklist_edit:
    pattern:  /{id}/edit
    defaults: { _controller: "PILTaskerBundle:CheckList:edit" }

checklist_update:
    pattern:  /{id}/update
    defaults: { _controller: "PILTaskerBundle:CheckList:update" }
    requirements: { _method: post|put }

checklist_delete:
    pattern:  /{id}/delete
    defaults: { _controller: "PILTaskerBundle:CheckList:delete" }
    requirements: { _method: post|delete }
